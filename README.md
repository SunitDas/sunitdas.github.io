# Sunit Das — Personal Website

A static site (plain HTML/CSS/JS, no build step) with pages: Home, Research, Publications, Academic Background, Contact.

## Deploy on GitHub Pages (free, ~5 minutes)

1. Create a GitHub account at github.com if you don't have one.
2. Create a new **public** repository, e.g. named `sunitdas.github.io` (using your exact GitHub username gives you a clean root URL) — or any name like `personal-site`.
3. Upload all files in this folder (`index.html`, `research.html`, `publications.html`, `background.html`, `contact.html`, the `css/` folder, and the `js/` folder) to that repository — either by dragging them into the GitHub web UI ("Add file" → "Upload files") or via `git push`.
4. In the repository, go to **Settings → Pages**.
5. Under "Build and deployment", set Source to **Deploy from a branch**, branch `main`, folder `/ (root)`. Save.
6. Wait 1–2 minutes. Your site will be live at:
   - `https://<your-username>.github.io/` (if the repo is named `<your-username>.github.io`), or
   - `https://<your-username>.github.io/<repo-name>/` otherwise.

## Updating content later

Every file is plain text — edit directly on GitHub (pencil icon on any file) or locally and push. No rebuild step needed.

## Notes

- The CV on the Academic Background page currently links to a "request by email" button since no CV file was available to embed. Add a `CV_Sunit_Das.pdf` to this folder and link it directly once you have it.
- Profile photo and a few figure images are currently hot-linked from the original Google Sites host. Replace with your own hosted copies if you want full independence from Google Sites.
